mainmodule adhydro
{
  extern module mesh_element;
  
  mainchare ADHydro
  {
    entry                        ADHydro(CkArgMsg* msg);
    initproc                void setLoadBalancingToManual(void);
    entry [reductiontarget] void doTimestep(double dtNew);
    entry                   void outputPartitioning(CkReductionMsg *msg);
    
    readonly int meshProxySize;
  };
};
